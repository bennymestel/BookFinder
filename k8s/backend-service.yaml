apiVersion: v1
kind: Service
metadata:
  name: backend  # This must match exactly what your app is trying to connect to
spec:
  selector:
    app: bookfinder-backend  # This should match your deployment selector
  ports:
  - port: 5000
    targetPort: 5000